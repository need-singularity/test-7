[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "tecs-rs"
version = "0.1.0"
requires-python = ">=3.10"

[tool.maturin]
python-source = "../../python"
module-name = "tecs.tecs_rs"
